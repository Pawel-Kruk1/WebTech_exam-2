const startButton = document.getElementById('startButton');

startButton.addEventListener('click', () => {
    window.location.href = "quiz1.html";
});
// ***SCRIPT EXPLANATION***
//const startButton = document.getElementById('startButton');
//document.getElementById('startButton'): This line finds an HTML element with the ID of startButton. This likely refers to a <button id="startButton">...</button> element in the HTML file.
//const startButton = ...: The result (the button element) is stored in a constant variable named startButton.